# 루트 FYI 는 agent RAG 엔진의 얇은 게이트웨이입니다.
# 자체 RAG 의존성(langchain 0.3 / faiss / ollama 직접 사용 / 다형식 로더)은 제거하고,
# agent 패키지를 editable 로 설치해 그 스택(langchain 1.x 등)을 그대로 사용합니다.
#
# 설치:  pip install -r requirements.txt
# agent 의 런타임 의존성은 ./agent/pyproject.toml 에서 함께 끌어옵니다.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7

-e ./agent
